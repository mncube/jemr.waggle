% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleanFall24.R
\name{identify_bottom_percentiles}
\alias{identify_bottom_percentiles}
\title{Flag bottom‐percentile students (optionally by grade)}
\usage{
identify_bottom_percentiles(
  data,
  percentiles,
  id_var = "STUDENT_BUSINESS_IDENTIFIER",
  score_var = "ASSESS_SCORE",
  group_var = NULL
)
}
\arguments{
\item{data}{A data.frame (e.g. your matched dataset) containing at least
the student ID column and, if using by_grade, the grade column.}

\item{percentiles}{Numeric vector of percentiles to flag
(e.g. c(0.2, 0.3) or c(20, 30)).}

\item{id_var}{Name of the student‐ID column (default "STUDENT_BUSINESS_IDENTIFIER").}

\item{score_var}{Name of the score variable in cleanFall24()
(default "ASSESS_SCORE_fstd").}

\item{group_var}{Optional name of a grouping column (e.g. "GRADE").
If non‐NULL, thresholds are computed separately within each group.}
}
\value{
\code{data} with new logical columns \code{bottom_20}, \code{bottom_30}, etc.,
indicating whether each student falls in that bottom percentile—
either overall or within their grade.
}
\description{
Flag bottom‐percentile students (optionally by grade)
}
